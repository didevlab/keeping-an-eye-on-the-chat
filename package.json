{
  "name": "keeping-an-eye-on-the-chat",
  "version": "0.1.0",
  "private": true,
  "description": "Lightweight desktop overlay for chat.",
  "main": "src/main.js",
  "scripts": {
    "start": "electron .",
    "start:overlay": "OVERLAY_DEBUG=0 DIAGNOSTICS=0 electron .",
    "start:diag": "OVERLAY_DEBUG=1 DIAGNOSTICS=1 electron ."
  },
  "devDependencies": {
    "electron": "^28.2.0"
  }
}
